const INITIAL_STATE = {
  lastConversion: null,
};

const historyReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default historyReducer;
